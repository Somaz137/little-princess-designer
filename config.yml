# Use DecapBridge PKCE auth (required)
backend:
  name: git-gateway
  repo: Somaz137/little-princess-designer
  branch: main
  auth_type: pkce
  base_url: https://auth.decapbridge.com
  auth_endpoint: /sites/6a40028d-8569-4023-a050-8b533a65ff01/pkce
  auth_token_endpoint: /sites/6a40028d-8569-4023-a050-8b533a65ff01/token
  gateway_url: https://gateway.decapbridge.com

  # See who did what (optional)
  commit_messages:
    create: Create {{collection}} "{{slug}}" - {{author-name}} <{{author-login}}> via DecapBridge
    update: Update {{collection}} "{{slug}}" - {{author-name}} <{{author-login}}> via DecapBridge
    delete: Delete {{collection}} "{{slug}}" - {{author-name}} <{{author-login}}> via DecapBridge
    uploadMedia: Upload "{{path}}" - {{author-name}} <{{author-login}}> via DecapBridge
    deleteMedia: Delete "{{path}}" - {{author-name}} <{{author-login}}> via DecapBridge
    openAuthoring: Message {{message}} - {{author-name}} <{{author-login}}> via DecapBridge

# Enable PKCE fields (optional, recommended)
auth:
  email_claim: email
  first_name_claim: first_name
  last_name_claim: last_name
  avatar_url_claim: avatar_url

# Better Decap + Bridge logo (optional)
logo_url: https://decapbridge.com/decapcms-with-bridge.svg

# Add site links in Decap CMS (optional)
site_url: https://littleprincessdesigner.netlify.app

media_folder: "uploads/products"
public_folder: "/uploads/products"

collections:
  - name: "products"
    label: "Product Catalog"
    files:
      - file: "products.json"
        label: "Product Catalog"
        name: "catalog"
        fields:
          - label: "Products"
            name: "products"
            widget: "list"
            fields:
              - { label: "ID", name: "id", widget: "string" }
              - { label: "Name", name: "name", widget: "string" }
              - { label: "Price (USD)", name: "price", widget: "number", value_type: "float" }
              - label: "Category"
                name: "category"
                widget: "select"
                options: ["girls", "boys", "baby"]
              - label: "Sizes"
                name: "sizes"
                widget: "select"
                multiple: true
                options: ["0-3M","3-6M","6-12M","12-18M","2T","3T","4T","5","6","7","8"]
              - { label: "Description", name: "description", widget: "text" }
              - { label: "Photo", name: "image", widget: "image", required: false }
